<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Day">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="progressBarBackgroundColor">@color/primaryLightColor</item>
        <item name="progressBarIndicatorColor">@color/primaryDarkColor</item>
        <item name="customBackground">@color/primaryBackgroundColor</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="AppTheme.Night">
        <item name="colorPrimary">@color/secondaryColor</item>
        <item name="colorPrimaryDark">@color/secondaryDarkColor</item>
        <item name="colorAccent">@color/secondaryLightColor</item>
        <item name="progressBarBackgroundColor">@color/secondaryLightColor</item>
        <item name="progressBarIndicatorColor">@color/secondaryDarkColor</item>
        <item name="customBackground">@color/secondaryBackgroundColor</item>
        <item name="colorOnBackground">@color/secondaryBackgroundColor</item>
        <item name="android:textColor">@color/secondaryTextColor</item>
    </style>

</resources>
